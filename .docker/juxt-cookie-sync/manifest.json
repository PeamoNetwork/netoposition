{
	"manifest_version": 2,
	"name": "Neto cookie sync",
	"version": "1.0",

	"description": "NETO DEV ONLY: syncs your access_token to olv domains",

	"browser_action": {
		"default_title": "Juxt cookie sync"
	},

	"background": {
		"scripts": [ "juxt.js" ]
	},

	"permissions": [
		"cookies",
		"*://*.olv.ixchats.com/",
		"*://*.ixchats.com/"
	],

	"browser_specific_settings": {
		"gecko": {
			"id": "juxt-dev-cookie-sync@ixchats.com",
			"data_collection_permissions": {
				"required": ["none"]
			}
		}
	}
}
